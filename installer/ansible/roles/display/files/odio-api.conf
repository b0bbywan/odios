# Ordered and restarted with odio-api. default.target is named too, or the
# target orders itself after the kiosk and odio-api closes a cycle at boot.
[Unit]
After=default.target odio-api.service
PartOf=odio-api.service
